import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'week12_Assignment',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
